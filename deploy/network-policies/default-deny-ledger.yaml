apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: default-deny-ledger-ingress
  namespace: payments

spec:

  podSelector:
    matchLabels:
      app: ledger-api

  policyTypes:
    - Ingress
